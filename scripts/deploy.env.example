# Salin ke deploy.env lalu sesuaikan (scripts/*.sh akan membaca file ini)
DB_NAME=jeqdb
DB_USER=jeqoperator
DB_PASSWORD='Jasindo@123'
DB_PORT=5432

# true = jalankan seed.sql setelah schema
WITH_SEED=true

BACKEND_PORT=4000
FRONTEND_PORT=8080
USGS_SYNC_INTERVAL_MS=900000
