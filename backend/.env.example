PORT=4000
# Docker Compose: host.docker.internal → PostgreSQL di mesin host
# npm run lokal: ganti ke localhost
DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:5432/jeq_platform
USGS_SYNC_INTERVAL_MS=900000
